info:
  name: download
  type: http
  seq: 5

http:
  method: GET
  url: "{{env}}1773284034495-Screenshot 2023-11-10 172022.png"
  auth: inherit

runtime:
  variables:
    - name: env
      value: http://localhost:3000/api/files

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
